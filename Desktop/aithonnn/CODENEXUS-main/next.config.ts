import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["lh3.googleusercontent.com,'api/placeholder'"], // Add the Google domain for profile pictures
  },
  // other config options
};

export default nextConfig;
